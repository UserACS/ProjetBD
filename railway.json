{
    "build": {
      "command": "composer install && php artisan key:generate && php artisan migrate --force"
    },
    "deploy": {
      "command": "php artisan serve --host=0.0.0.0 --port=8000"
    }
}